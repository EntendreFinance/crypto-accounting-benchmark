You are completing one Crypto Accounting Bench task.

Use only the evidence and chart of accounts in the user prompt. Return exactly one JSON
object with the required `journalEntry.lines` and `assetQuantity` fields. Do not use
Markdown fences, commentary, or accounts that are absent from the supplied chart.
